# 07 — Amend CLAUDE.md

Status: done

- "Toggling one never recomputes anything" gains its exception: the chain total
  is a per-frame product over the *visible* stages, and the reason — a fixed
  total lies to a pilot who switched a family off, while an array product is
  arithmetic, not analysis.
- Record the anchoring rule (gyro on the data, D-term off its own reference),
  the fill and its lack of a threshold, the dwell lane, and the two chain hues,
  in the filter overlay geometry section and the decisions table.
